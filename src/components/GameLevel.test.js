import React from 'react';
import {cleanup, fireEvent, render} from '@testing-library/react';
import GameLevel from '../components/GameLevel';

afterEach(cleanup);

describe('Main Menu clickEvents | Menu changes', () => {
    it.skip("Should | return ... | When | .. ", () => {
    })
});
